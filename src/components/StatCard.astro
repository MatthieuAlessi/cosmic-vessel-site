---
interface Props {
  value: string;
  label: string;
  description: string;
  icon?: string;
  class?: string;
}
import { Icon } from 'astro-icon/components';
import CornerOrnament from '@components/CornerOrnament.astro';
const { value, label, description, icon, class: className = '' } = Astro.props;
---

<div class:list={['group relative p-8 lg:p-10 flex flex-col justify-between gap-10 border border-primary-700/20 backdrop-blur-xs rounded-xl', className]}>
 <CornerOrnament size={34} corner="top-left" class="opacity-30" />
  <CornerOrnament size={34} corner="bottom-right" class="opacity-30" />
  <!-- Ligne haute : label coloré + index -->
  <div class="flex items-start justify-between">
    <span class="font-ui text-2xs tracking-[0.25em] uppercase text-primary">{label}</span>
    {icon && (
      <span class="flex items-center justify-center w-10 aspect-square rounded-full bg-primary text-background shrink-0">
        <Icon name={icon} class="w-5 h-5" />
      </span>
    )}
  </div>

  <!-- Bas : valeur + description -->
  <div>
    <p class="font-display text-5xl lg:text-6xl text-primary-100 leading-none mb-4">{value}</p>
    <p class="text-foreground-500 font-body text-sm leading-relaxed">{description}</p>
  </div>
</div>
