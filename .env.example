# Cloudflare Web Analytics — token du site (public, visible dans le HTML).
# À créer sur dash.cloudflare.com > Analytics & Logs > Web Analytics > Add a site.
# À définir dans Netlify > Site settings > Environment variables (scope: Builds).
# Non défini => aucun beacon injecté (cas du dev local).
PUBLIC_CF_BEACON_TOKEN=
